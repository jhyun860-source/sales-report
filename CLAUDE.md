# SalesDash (MyWhiskyTable) — 프로젝트 컨텍스트

마이위스키테이블 6개 지점의 매출·인건비·정산 관리 웹앱.
일일 매출 보고, 직원 인건비/인센티브 계산, 주류 재고, 월 정산을 다룬다.

---

## 작업 규칙 (모든 수정에 적용)

- 요청받은 부분만 고친다. 그 외 코드는 건드리지 않는다.
- 리팩터링, 포맷 정리, 변수명 변경, 주석 정리, import 정렬 등
  요청하지 않은 변경은 하지 않는다. 눈에 거슬려도 그대로 둔다.
- 개선 아이디어가 보이면 코드를 바꾸지 말고 말로 먼저 제안한다.
- 파일 전체를 다시 쓰지 말고 필요한 줄만 수정한다.
- 수정 후 변경된 파일과 줄 수를 알려준다.
- DB 데이터는 요청이 있을 때만 건드린다.
- 푸시는 내 확인 후에 한다.

---

## 지점 정보

| branchId | 지점명 | workType |
|---|---|---|
| 1 | 선릉점 | MON_FRI |
| 2 | 대치점 | **MON_SAT** |
| 3 | 삼성점 | MON_FRI |
| 4 | 문정1호점 | MON_FRI |
| 5 | 문정2호점 | MON_FRI |
| 6 | 방이점 | MON_FRI |

> **주의**: `branchId=2`는 **항상 대치점**이다. 문정1호점이 아니다.
> 이전 Manus 환경에서 이 라벨을 자주 잘못 표시했으므로,
> 지점 확인은 **표시 이름이 아니라 ID 기준**으로 할 것.

---

## 계산 규칙 (핵심 — 반드시 준수)

### 임대료
- **항상 MON_SAT 영업일수로 나눈다.** 지점 설정값과 무관하게 토요일 영업을 포함한다.

### 점장인건비
- 지점별 `workType` 일수로 나눈다.
- **임대료와 점장인건비의 workType 계산은 서로 다르다. 절대 혼용하지 말 것.**

### 여직원인건비
- `staffDailyWage × 출근일수`

### 알바
- `시급 × 실제 출퇴근 시간`
- 고정값(예: 1인당 8시간) 폴백 공식을 쓰면 안 된다. 아래 미해결 버그 참고.

### 영업인센
- 직접 입력만 받는다. 자동 계산하지 않는다.

### 시제(POS)
- `posStart − 지출 + 시제입금`
- 현금매출은 제외한다.

### 카드수수료
- `commissionExpense` 약 17%는 **정상 설정값**이다. 버그가 아니다.
- 순수 카드 수수료뿐 아니라 세금·기타 잡비를 모두 포함한 비율이다.

### 주간 인센티브 주 경계
- `getBaseMondayOfMonth`는 **"1일이 포함된 주의 월요일"** 을 기준으로 한다.
- "그 달의 첫 번째 월요일"이 아니다. 월초 날짜가 첫 월요일보다 앞설 때를 처리하기 위함.

### batchSave
- `cashDeposit`, `posEndAmount`, `expenses`는 batchSave 중 **반드시 보존**해야 한다.
- 덮어쓰면 매출보고 데이터가 날아간다.

### 시급 미대상 (wageExempt)
- `staffIncentives`의 `wageExempt` 필드로 관리.
- 해당 직원은 `staffCount`/시간 합계에서 **제외**되지만,
  주류 지출과 매출 인센티브에는 **포함**된다.
- 출근·인센티브는 인정하되 시급 계산에서만 빼야 하는 직원을 위한 기능.

---

## 인프라

- **배포**: Railway — `mywhiskytable.up.railway.app`
  - GitHub `main` 브랜치 푸시 시 **자동 배포**
  - 반영 여부는 Railway Deployments 화면에서 ACTIVE 커밋으로 확인
- **저장소**: GitHub `jhyun860-source/sales-report`
- **DB**: TiDB Cloud
- **주요 파일**
  - `drizzle/schema.ts`
  - `server/restoreRouter.ts`
  - `server/routers.ts`
  - `client/src/pages/TableReport.tsx`

### 관리자 엔드포인트
- 복원/마이그레이션: `/admin/restore-db?mode=<MODE>&key=...`
  - **`mode`를 `key`보다 앞에 둘 것**
- 정산 보정: `adjustsettlement`
- 시급 면제 추가: `addwageexempt`

### 플랫폼 제약
- `navigator.vibrate()`는 iPhone/iOS에서 동작하지 않는다. 진동 피드백 기능은 해당 플랫폼에서 무효.

---

## 정책 결정

### 날짜 선택 (2026-09)
- 앱이 날짜를 자동으로 "오늘"로 바꾸지 않는다. 사용자가 고른 날짜를 유지한다.
- 이유: 새벽 입력이 다음 날로 저장되던 버그 때문.
- 대신 새 영업일 시작 시 직원이 `[오늘]` 버튼을 눌러야 한다.

---

## 미해결 이슈

### 1. 알바 시급 자동저장 버그 (최우선)
- 실제 출퇴근 시간 대신 **"1인당 8시간" 폴백 공식이 발동**하는 문제.
- 반복 재발 중이며 근본 원인 미해결. 가장 최근 8/28 대치점에서 발생.
- ref 기반 수정은 효과가 지속되지 않았음.
- **현재 임시 대응**: 모든 입력 완료 후 수동으로 "저장"을 누름. 오류분은 `adjustsettlement`로 보정.

### 2. POS 시작금 오류
- POS 시작금에 직전 마감금이 아니라 **이전 값(예: 전전일 마감금)** 이 들어가는 버그.
- 코드 수정 아직 미진행.

### 3. 주류 재고 화면 지점 선택이 새로고침 시 초기화
- 지점을 바꾼 뒤 새로고침하면 선택한 지점이 아니라 **주소창 `?branchId=` 값의 지점**으로 되돌아간다.
- 원인: `client/src/pages/LiquorStockReport.tsx:109`의 초기값 우선순위가
  `쿼리파라미터 → localStorage` 순서다. 반면 지점 선택 시(`:637`, `:953`)에는
  localStorage만 갱신하고 URL은 그대로 두기 때문에, 새로고침하면 낡은 쿼리파라미터가 이긴다.
- 수정 방향: 지점 변경 시 URL 쿼리도 함께 바꾸거나, 초기값 우선순위를 뒤집는다.
- 코드 수정 아직 미진행.

---

## 감사(audit) 이력

- 8월 대치점: 완료 및 보정. 날짜가 잘못 기록된 알바 인건비 다수 발견 → `adjustsettlement`로 건별 수정.
- 8월 선릉점, 삼성점, 문정1호점, 문정2호점, 방이점: 오류 없음.

---

## 작업 시 유의사항

- 계산 로직은 숫자 하나만 틀려도 정산 전체가 어긋난다. 수정 전후로 반드시 검증할 것.
- 지점별 `workType` 차이를 항상 확인할 것 (대치점만 MON_SAT).
- 기존에 정상 동작하던 필드를 덮어쓰지 않도록 주의할 것.
